# goit-algo-hw-05

Домашнє завдання до теми **“Алгоритми пошуку”**.

У репозиторії реалізовано 3 завдання:

1. **HashTable** + метод `delete` (видалення пари ключ–значення)
2. **Двійковий пошук** для відсортованого масиву дробових чисел (ітерації + верхня межа)
3. **Порівняння алгоритмів пошуку підрядка**: Боєр–Мур, Кнут–Морріс–Пратт, Рабін–Карп (емпірично через `timeit`) на двох текстах

---

## 📌 Структура репозиторію

```text
goit-algo-hw-05/
├── task_1.py                 # HashTable + delete
├── task_2.py                 # Binary search (iterations + upper_bound)
├── task_3.py                 # Benchmark substring search (BM / KMP / RK) + висновки
├── data/
│   ├── article_1.txt          # Текст 1 (стаття 1)
│   └── article_2.txt          # Текст 2 (стаття 2)
├── README.md
└── .gitignore
```

---

## ✅ Task 1 — HashTable: метод `delete`

### Опис

Додано метод `delete(key)`, який:

- видаляє пару **ключ–значення** з хеш-таблиці
- повертає `True`, якщо елемент видалено
- повертає `False`, якщо ключ не знайдено

### ▶️ Запуск (Windows рекомендовано)

```bash
py .\task_1.py
```

### ✅ Приклад очікуваного виводу

```text
apple: 10
delete banana: True
banana: None
delete missing: False
```

---

## ✅ Task 2 — Двійковий пошук (float): `iterations` + `upper_bound`

### Опис

Реалізовано двійковий пошук для відсортованого масиву дробових чисел.

Функція повертає кортеж:

- `iterations` — кількість ітерацій пошуку
- `upper_bound` — найменший елемент масиву, який **>= target**

  - якщо такого елемента немає → `None`

### ▶️ Запуск

```bash
py .\task_2.py
```

### ✅ Приклад виводу

```text
target=0.1 -> iterations=2, upper_bound=0.5
target=1.2 -> iterations=3, upper_bound=1.2
target=1.21 -> iterations=3, upper_bound=2.3
target=3.0 -> iterations=3, upper_bound=3.3
target=10.1 -> iterations=3, upper_bound=10.1
target=99.9 -> iterations=3, upper_bound=None
```

---

## ✅ Task 3 — Порівняння пошуку підрядка (BM / KMP / RK) + `timeit`

### Опис

Порівняно 3 алгоритми пошуку підрядка:

- **Boyer–Moore**
- **Knuth–Morris–Pratt (KMP)**
- **Rabin–Karp**

Тестування проведено на **2 текстових файлах** (`article_1.txt`, `article_2.txt`) для двох підрядків:

- **існуючий** (реально є в тексті)
- **вигаданий** (відсутній)

Вимірювання виконано через **`timeit`** (береться **найкращий час** з кількох повторів: `min(repeat)`).

### ▶️ Запуск

```bash
py .\task_3.py
```

---

## 📊 Результати (емпіричні дані)

Нижче — результати запуску `task_3.py` (секунди):

### Текст 1: `article_1.txt`

- Existing pattern: `'ВИКОРИСТАННЯ'`
- Missing pattern : `'___THIS_SUBSTRING_SHOULD_NOT_EXIST_123456___'`

| Algorithm   | Existing (s) | Missing (s) |
| ----------- | -----------: | ----------: |
| Boyer–Moore |     0.000002 |    0.000069 |
| KMP         |     0.000002 |    0.000910 |
| Rabin–Karp  |     0.000004 |    0.004054 |

**Найшвидший для існуючого:** Boyer–Moore
**Найшвидший для відсутнього:** Boyer–Moore

---

### Текст 2: `article_2.txt`

- Existing pattern: `'структури'`
- Missing pattern : `'___THIS_SUBSTRING_SHOULD_NOT_EXIST_123456___'`

| Algorithm   | Existing (s) | Missing (s) |
| ----------- | -----------: | ----------: |
| Boyer–Moore |     0.000004 |    0.000159 |
| KMP         |     0.000004 |    0.001883 |
| Rabin–Karp  |     0.000010 |    0.007010 |

**Найшвидший для існуючого:** Boyer–Moore
**Найшвидший для відсутнього:** Boyer–Moore

---

### Загальний підсумок (середнє по 2 текстах і 2 типах підрядків)

- Boyer–Moore: **0.000058 s**
- KMP: **0.000700 s**
- Rabin–Karp: **0.002770 s**

🏁 **Overall fastest: Boyer–Moore**

---

## ✅ Висновки

1. **Boyer–Moore** показав найкращі результати як для **існуючих**, так і для **відсутніх** підрядків на обох текстах.
   Причина: алгоритм робить “стрибки” по тексту завдяки евристиці “поганого символу”, тому часто пропускає великі ділянки.

2. **KMP** має стабільну теоретичну складність **O(n + m)** і добре підходить для гарантованої лінійної поведінки, але на цих текстах програв BM через меншу кількість “пропусків” та більшу кількість порівнянь.

3. **Rabin–Karp** виявився найповільнішим у тестах. Хоча хешування може бути ефективним у деяких сценаріях (наприклад, при пошуку багатьох патернів), тут додаткові обчислення хешів та перевірки збігів дали гіршу практичну продуктивність.

**Фінальний висновок:** за отриманими емпіричними даними найшвидшим алгоритмом для цих двох текстів є **Boyer–Moore**.

---

## 🧰 Вимоги

- Python **3.12+**
- Windows PowerShell (або будь-який термінал)
- VS Code (або інше IDE)

---
